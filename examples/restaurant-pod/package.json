{
  "name": "@gemmapod/example-restaurant-pod",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Tiny origin daemon that streams STATE_SNAPSHOT cart updates over DARTC. The visitor's page renders the cart beside the chat without minting a new topic.",
  "scripts": {
    "dev": "tsx src/origin.ts",
    "start": "tsx src/origin.ts"
  },
  "dependencies": {
    "@gemmapod/dartc": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
